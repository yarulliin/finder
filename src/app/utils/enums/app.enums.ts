export const enum LocalStorageKeys {
  AUTH = 'auth',
  FILMS = 'films',
  SESSION_ID = 'sessionId',
  THEME = 'theme',
}

export enum RoutesEnum {
  EMPTY = '',
  AUTH = 'auth',
  FILMS = 'films',
  UNKNOWN = '**'
}
