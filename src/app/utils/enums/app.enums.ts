export enum LocalStorageKeys {
  AUTH = 'auth',
  FILMS = 'films',
  SESSION_ID = 'sessionId',
}

export enum RoutesEnum {
  EMPTY = '',
  AUTH = 'auth',
  FILMS = 'films',
  WELCOME = 'welcome',
  UNKNOWN = '**'
}
